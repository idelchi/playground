name: godyl

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-24.04
    container: idelchi/devenv:latest
    steps:
      - run: whoami
      - run: id
